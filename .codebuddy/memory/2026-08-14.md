# 2026-08-14

## 新人教程文档
- 用户要求：针对 `cmd/osbuilder/osbuilder.go` 的复杂设计，严格按设计生成循序渐进新人教程。
- 产出：`docs/newcomer-tutorial.md`，9 步渐进式（第0步最小根命令 → 第1步 Options 三阶段 → 第2步全局配置 AddFlags → 第3步 viper/BindPFlags/OnInitialize + `--config` bug 警告 → 第4步 Factory 注入 → 第5步 PersistentPreRunE/PostRunE 钩子 → 第6步 插件机制与三层入口 → 第7步 命令分组/alpha/options 帮助 → 第8步 领域配置 es 范例 → 第9步 flag 规范化 + command headers → 收尾设计原则表）。
- 教程以原版 `internal/osbuilder/cmd/cmd.go` 为权威蓝本，可运行参考实现指向 `examples/osbuilder-simplified/`。明确标注原版 `cmd.go:201` 的 `--config` 构造期求值 bug 及简化版延迟求值修复（不回退）。
- 设计原则回顾表强调：入口极薄、三层构造可测试、Options 三阶段、配置分层、viper 优先级、Factory 注入、横切钩子、命令分组、插件扩展。
